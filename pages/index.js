import TextEditor from "@/components/TextEditor";

const Home = () => {
  return (
    <div style={{maxWidth: "60%", marginInline: "auto"}}>
      <TextEditor />
    </div>
  )
}

export default Home;